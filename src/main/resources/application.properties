# 카카오 설정
kakao.client_id=${KAKAO_CLIENT_ID}
kakao.client_secret=${KAKAO_CLIENT_SECRET}
kakao.redirect_uri=https://port-0-rangers-be-m1dcjhj379a3cf53.sel4.cloudtype.app/api/kakao/callback
kakao.get_code_path=https://kauth.kakao.com/oauth/authorize?response_type=code&client_id=


# MySQL 데이터베이스 연결 설정
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Redis 연결 설정
spring.redis.host=${SPRING_REDIS_HOST}
spring.redis.port=6379

# JWT 설정
jwt.secret=${JWT_SECRET}

# mail 설정
spring.mail.host=${MAIL_HOST}
spring.mail.port=587
spring.mail.username=${MAIL_ID}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
